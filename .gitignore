# Go binary
/webssh
/webssh.exe
/webssh-setup.exe
/webssh-launcher.exe

# Release archives (rebuild with `make dist-all`)
/dist/

# Generated icon artefacts (rebuild with `make icons` / `make syso`)
assets/png/
cmd/webssh/*.syso
cmd/webssh-setup/*.syso
cmd/webssh-launcher/*.syso
# Embedded copy webssh-setup builds against - see `make build-windows-setup`
cmd/webssh-setup/assets/webssh-launcher.exe

# Frontend
web/node_modules/
web/dist/

# Knowledge-graph artefacts (rebuild with /graphify)
graphify-out/

# Local data / editor
*.db
*.db-*
.DS_Store
